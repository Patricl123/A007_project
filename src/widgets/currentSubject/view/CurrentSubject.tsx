import type { FC } from 'react';
import { Container } from 'shared/ui';

export const CurrentSubject: FC = () => {
    return (
        <section>
            <Container>
                <h1></h1>
            </Container>
        </section>
    );
};
